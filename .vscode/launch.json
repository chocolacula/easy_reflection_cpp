{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "lldb",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [
        "-c",
        "${workspaceFolder}/example/config.yaml"
      ],
      "cwd": "${workspaceFolder}",
      "env": {
        "PATH": "$PATH:${command:cmake.launchTargetDirectory}"
      }
    }
  ]
}
